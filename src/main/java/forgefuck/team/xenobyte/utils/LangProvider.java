package forgefuck.team.xenobyte.utils;

/** English-only UI strings (legacy code called {@code get(eng, rus)}; now a single string). */
public class LangProvider {

    public String get(String text) {
        return text;
    }
}